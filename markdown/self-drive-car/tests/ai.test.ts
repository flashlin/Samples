const p1 = {
  x: -1,
  y: -6
};

const p2 = {
  x: -1,
  y: 3
};

const c = {
  x: 0,
  y: 0
};

const radius = 5;
const startAngle = 90 * Math.PI / 180;
const endAngle = 180 * Math.PI / 180;




test('ai1', () => {
  expect(1).toBe(1);
});
