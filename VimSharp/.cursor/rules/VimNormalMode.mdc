---
description: 
globs: VimNormalMode.cs
alwaysApply: false
---
規則:
- 當按下向右鍵並且已經到達行尾時，CursorY 游標將不再自動移動到下一行
- 游標移動到本行最後一個字上的判斷, 假設最後一個字的位置是 x, 要先判斷最後一個字是否為 '\0'? 
  如果是的話, x 要再往前移動一次(-1), 如果不是, 則就是 x.
