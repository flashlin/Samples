export function translateToEnAsync(text: string): Promise<string> {
   return Promise.resolve(text);
}

export function translateToZhAsync(text: string): Promise<string> {
   return Promise.resolve(text);
}