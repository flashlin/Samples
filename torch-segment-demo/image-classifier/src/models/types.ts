export interface IImageForClassifyData {
    name: string;
    imageData: string;
}

export interface IImageForClassify {
    name: string;
    imageUrl: string;
}

export interface ILabel {
    id: number;
    label: string;
}
