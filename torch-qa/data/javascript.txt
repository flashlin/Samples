
```javascript
const stagingProxyHandler =  {
  set: (target, property, value, receiver)=> {
    console.log(value)
    return Reflect.set(target, property, value, receiver)
  }
}
window.dataLayer = new Proxy(window.dataLayer, stagingProxyHandler)
```